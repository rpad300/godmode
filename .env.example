# =============================================================================
# GodMode - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git - it contains secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port for the HTTP server (default: 3005)
PORT=3005

# Server request timeout in ms (default: 30000). Connections idle longer than this are closed.
# SERVER_TIMEOUT_MS=30000

# Graceful shutdown: wait up to this many ms for in-flight requests to finish (default: 15000).
# SERVER_DRAIN_TIMEOUT_MS=15000

# Supabase fetch timeout in ms (default: 30000). Fails fast on slow DB.
# SUPABASE_FETCH_TIMEOUT_MS=30000

# Circuit breaker: open after this many consecutive Supabase failures (default: 5).
# SUPABASE_CIRCUIT_THRESHOLD=5
# Cooldown in ms before trying again (default: 30000).
# SUPABASE_CIRCUIT_COOLDOWN_MS=30000

# LLM circuit breaker: open after this many consecutive LLM HTTP failures (default: 5).
# LLM_CIRCUIT_THRESHOLD=5
# Cooldown in ms before trying again (default: 60000).
# LLM_CIRCUIT_COOLDOWN_MS=60000

# Max JSON request body size in bytes (default: 2097152 = 2MB). Reject with 413 if exceeded.
# MAX_BODY_LENGTH=2097152

# Base URL of the application (used for email links, etc.)
# Example: http://localhost:3005 for local, https://godmode.example.com for prod
APP_URL=http://localhost:3005

# Environment: development, staging, production
NODE_ENV=development

# -----------------------------------------------------------------------------
# Ollama Configuration (Local AI - Recommended)
# -----------------------------------------------------------------------------
# Ollama provides free local AI processing. Install from https://ollama.ai
# These settings can also be configured via the Settings UI

# Ollama server address (default: localhost)
# OLLAMA_HOST=127.0.0.1

# Ollama server port (default: 11434)
# OLLAMA_PORT=11434

# -----------------------------------------------------------------------------
# Cloud LLM Providers (Optional - for cloud AI processing)
# -----------------------------------------------------------------------------
# You only need to configure the providers you want to use
# Leave empty or comment out if not using

# OpenAI (GPT-4, GPT-3.5, etc.)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic Claude
# Get your key at: https://console.anthropic.com/
CLAUDE_API_KEY=

# Google Gemini
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=

# xAI (Grok)
# Get your key at: https://console.x.ai/
XAI_API_KEY=

# DeepSeek
# Get your key at: https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# Kimi (Moonshot)
# Get your key at: https://platform.moonshot.cn/
KIMI_API_KEY=

# MiniMax
# Get your key at: https://api.minimax.chat/
MINIMAX_API_KEY=

# -----------------------------------------------------------------------------
# Supabase Configuration (Optional - for cloud sync and multi-user)
# -----------------------------------------------------------------------------
# Only required if you want cloud storage and multi-user support
# Get your credentials at: https://supabase.com/dashboard

# Supabase Project URL
# Example: https://yourproject.supabase.co
SUPABASE_URL=
SUPABASE_PROJECT_URL=

# Supabase Anonymous Key (safe for client-side, respects RLS)
SUPABASE_ANON_KEY=
SUPABASE_PROJECT_ANON_KEY=

# Supabase Service Role Key (server-side only, bypasses RLS)
# WARNING: Never expose this to the frontend!
SUPABASE_SERVICE_KEY=
SUPABASE_PROJECT_SERVICE_ROLE_KEY=

# Database connection string (optional - for migrations introspection and scripts)
# Get from Supabase Dashboard → Project Settings → Database → Connection string (URI)
# Used by: scripts/introspect-db.js to align migrations with live schema
# DATABASE_URL=

# Supabase Project ID (for management operations)
# SUPABASE_PROJECT_ID=

# Supabase Publishable Key (optional)
# SUPABASE_PUBLISHABLE_KEY=

# -----------------------------------------------------------------------------
# Email Service (Optional - for notifications and invites)
# -----------------------------------------------------------------------------
# Only required if you want email functionality

# Resend API Key (for sending emails)
# Get your key at: https://resend.com/
RESEND_API_KEY=

# Brave Search API Key (optional – enriches company analysis with web search)
# Get your key at: https://api-dashboard.search.brave.com/documentation
# BRAVE_API_KEY=

# -----------------------------------------------------------------------------
# Advanced Configuration (Usually not needed)
# -----------------------------------------------------------------------------

# Public URL for webhooks and callbacks
# PUBLIC_URL=

# Enable debug logging
# DEBUG=true
