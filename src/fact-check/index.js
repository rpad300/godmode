/**
 * Fact Check Module
 * Flow for analyzing facts and detecting conflicts as project content grows
 */

const { runFactCheck } = require('./FactCheckFlow');

module.exports = {
    runFactCheck
};
