//moved from server.js
const path = require('path');
const fs = require('fs');

const path = require('path');
const fs = require('fs');

module.exports = {
ensureNodeModulesPath,
    getRelativePath,
    ensureDirectoryExists,
    removeDirectoryIfExists,
    copyDirectory,
}